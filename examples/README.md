# Deshred Example

This example demonstrates how to consume transactions from
the LocalShred Lite proxy in real-time.

## Prerequisites

- Running `localshred-lite-proxy` with flag: `--grpc-service-port 9999`

## Usage

```bash
cargo run --example deshred
```
